# ── Core ──────────────────────────────────────────────────────────────────────
streamlit>=1.35.0
pandas>=2.0.0
matplotlib>=3.8.0
scikit-learn>=1.4.0

# ── Scraping (optional — comment out if not needed) ───────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0          # faster BS4 parser

# ── Dev / test (install with: pip install -r requirements.txt) ────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.4.0
